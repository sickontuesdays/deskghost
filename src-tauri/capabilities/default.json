{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the overlay and the main (picker/settings) window",
  "windows": ["overlay", "main"],
  "permissions": [
    "core:default",
    "autostart:default",
    "updater:default",
    "process:allow-restart"
  ]
}
